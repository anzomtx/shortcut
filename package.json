{
  "name": "shortcut",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --watch-preserve-output src/server.mjs",
    "start": "node src/server.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
